import React, { useMemo, useState } from 'react';
import { Building2, FileUp, X, CheckCircle2, AlertTriangle } from 'lucide-react';
import * as XLSX from 'xlsx';
import { User } from '../types';
import { mapSalesRows, mapStockRows, operationalDataService, parseCsv } from '../services/operationalDataService';

type Props={currentUser:User}; type ImportType='stock'|'sales';
const normalize=(v:string)=>v.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim();
const excelRows=async(file:File)=>{const wb=XLSX.read(await file.arrayBuffer(),{type:'array'});const ws=wb.Sheets[wb.SheetNames[0]];const matrix=XLSX.utils.sheet_to_json<any[]>(ws,{header:1,defval:''});const headerIndex=matrix.findIndex(r=>r.some((c:any)=>['placa','vendedor','familia','modelo'].includes(normalize(String(c)))));if(headerIndex<0)return [];const headers=matrix[headerIndex].map((h:any)=>normalize(String(h)));return matrix.slice(headerIndex+1).map(r=>{const o:Record<string,string>={};headers.forEach((h:string,i:number)=>{if(h)o[h]=String(r[i]??'')});return o}).filter(r=>Object.values(r).some(Boolean));};

const OperationalDataPanel:React.FC<Props>=({currentUser})=>{const[open,setOpen]=useState(false),[type,setType]=useState<ImportType>('stock'),[referenceDate,setReferenceDate]=useState(()=>new Date().toISOString().slice(0,10)),[file,setFile]=useState<File|null>(null),[rows,setRows]=useState<Record<string,string>[]>([]),[busy,setBusy]=useState(false),[message,setMessage]=useState<{kind:'ok'|'error';text:string}|null>(null);
const preview=useMemo(()=>!rows.length?[]:type==='stock'?mapStockRows(rows,referenceDate).slice(0,5):mapSalesRows(rows,referenceDate).slice(0,5),[rows,type,referenceDate]);
const handleFile=async(next:File|null)=>{setFile(next);setRows([]);setMessage(null);if(!next)return;try{const lower=next.name.toLowerCase();let parsed:Record<string,string>[]=[];if(lower.endsWith('.csv'))parsed=parseCsv(await next.text());else if(lower.endsWith('.xls')||lower.endsWith('.xlsx'))parsed=await excelRows(next);else{setMessage({kind:'error',text:'Envie CSV, XLS ou XLSX.'});return}setRows(parsed);if(!parsed.length)setMessage({kind:'error',text:'Não encontrei uma tabela reconhecível no arquivo.'});}catch{setMessage({kind:'error',text:'Não consegui ler esse arquivo. Verifique se ele abre normalmente no LibreOffice.'})}};
const importData=async()=>{if(!file||!rows.length)return;setBusy(true);setMessage(null);try{if(type==='stock'){const items=mapStockRows(rows,referenceDate),count=await operationalDataService.importStock(items,file.name,currentUser);setMessage({kind:'ok',text:`Estoque atualizado: ${count} veículos. Fotografia oficial de ${referenceDate}.`})}else{const items=mapSalesRows(rows,referenceDate),count=await operationalDataService.importSales(items,file.name,referenceDate,currentUser);setMessage({kind:'ok',text:`Mapa atualizado: ${count} registros, sem duplicar os já existentes.`})}setFile(null);setRows([]);window.dispatchEvent(new Event('dealmaster:operational-data-updated'));}catch(e:any){setMessage({kind:'error',text:e?.message||'Erro ao importar.'})}finally{setBusy(false)}};
if(!['admin','manager'].includes(currentUser.role))return null;
const reset=(t:ImportType)=>{setType(t);setFile(null);setRows([]);setMessage(null)};
return <><button onClick={()=>setOpen(true)} className="fixed bottom-5 left-5 z-[130] flex items-center gap-2 rounded-full border border-white/10 bg-zinc-900 px-4 py-3 text-sm font-semibold text-white shadow-2xl"><Building2 size={18}/> Dados da Loja</button>{open&&<div className="fixed inset-0 z-[220] overflow-y-auto bg-black/75 p-3 backdrop-blur-md" onClick={()=>setOpen(false)}><div onClick={e=>e.stopPropagation()} className="mx-auto mt-5 max-w-4xl rounded-[32px] border border-white/10 bg-zinc-950 shadow-2xl"><div className="flex items-center justify-between border-b border-white/10 p-6"><div><p className="text-xs font-semibold uppercase tracking-[.15em] text-zinc-500">Atualização diária</p><h3 className="mt-1 text-2xl font-semibold text-white">Dados do Outlet</h3><p className="mt-1 text-sm text-zinc-500">Dois arquivos separados. O estoque substitui a fotografia atual; o mapa preserva o histórico.</p></div><button onClick={()=>setOpen(false)} className="grid h-10 w-10 place-items-center rounded-full bg-white/[.06] text-zinc-400"><X size={18}/></button></div><div className="p-6"><div className="grid gap-3 md:grid-cols-2"><button onClick={()=>reset('stock')} className={`rounded-[24px] border p-5 text-left ${type==='stock'?'border-white bg-white text-black':'border-white/10 bg-white/[.03] text-white'}`}><p className="text-xs font-semibold uppercase opacity-60">Arquivo 1</p><p className="mt-2 text-xl font-semibold">📦 Atualizar Estoque</p><p className="mt-2 text-sm opacity-60">Padrão DealerNet: Família, Modelo, Placa, Ano, Cor, Preço Venda, KM, Dias Estoque e Custo.</p></button><button onClick={()=>reset('sales')} className={`rounded-[24px] border p-5 text-left ${type==='sales'?'border-white bg-white text-black':'border-white/10 bg-white/[.03] text-white'}`}><p className="text-xs font-semibold uppercase opacity-60">Arquivo 2</p><p className="mt-2 text-xl font-semibold">💰 Atualizar Faturamento</p><p className="mt-2 text-sm opacity-60">Mapa diário de vendas, vendedor, faturamento, margem e captura.</p></button></div><div className="mt-5 grid gap-4 md:grid-cols-[220px_1fr]"><div><label className="mb-2 block text-xs text-zinc-500">Data de referência</label><input type="date" value={referenceDate} onChange={e=>setReferenceDate(e.target.value)} className="h-12 w-full rounded-2xl border border-white/10 bg-white/[.05] px-4 text-white"/></div><div><label className="mb-2 block text-xs text-zinc-500">Arquivo do DealerNet / LibreOffice</label><label className="flex h-12 cursor-pointer items-center justify-between rounded-2xl border border-dashed border-white/15 bg-white/[.035] px-4 text-sm text-zinc-300"><span className="truncate">{file?.name||'Selecionar CSV, XLS ou XLSX'}</span><FileUp size={17}/><input type="file" accept=".csv,.xls,.xlsx" className="hidden" onChange={e=>handleFile(e.target.files?.[0]||null)}/></label></div></div>{rows.length>0&&<div className="mt-5 rounded-[24px] border border-white/10 bg-white/[.03] p-4"><div className="flex justify-between"><div><p className="text-sm font-medium text-white">Arquivo reconhecido</p><p className="text-xs text-zinc-500">{rows.length} linhas · {preview.length? 'mapeamento pronto':'revise o formato'}</p></div><span className="rounded-full bg-emerald-500/10 px-3 py-1 text-xs text-emerald-400">Prévia OK</span></div>{type==='stock'&&<div className="mt-4 grid grid-cols-2 gap-2 text-xs md:grid-cols-4">{preview.map((i:any)=><div key={i.id} className="rounded-xl bg-black/20 p-2 text-zinc-300">{i.plate||'-'}<br/><span className="text-zinc-500">{i.vehicle} · {i.stockDays} dias</span></div>)}</div>}</div>}{message&&<div className={`mt-5 flex gap-3 rounded-2xl border p-4 text-sm ${message.kind==='ok'?'border-emerald-500/20 bg-emerald-500/[.07] text-emerald-300':'border-red-500/20 bg-red-500/[.07] text-red-300'}`}>{message.kind==='ok'?<CheckCircle2 size={18}/>:<AlertTriangle size={18}/>}<span>{message.text}</span></div>}<div className="mt-6 flex justify-end"><button disabled={!file||!rows.length||busy} onClick={importData} className="rounded-2xl bg-white px-6 py-3 text-sm font-semibold text-black disabled:opacity-30">{busy?'Atualizando...':type==='stock'?'Atualizar estoque':'Atualizar faturamento'}</button></div></div></div></div>}</>};
export default OperationalDataPanel;