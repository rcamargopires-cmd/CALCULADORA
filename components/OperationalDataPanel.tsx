import React, { useMemo, useState } from 'react';
import { AlertTriangle, Building2, CheckCircle2, FileUp, X } from 'lucide-react';
import * as XLSX from 'xlsx';
import { OperationalPerformanceSeller, OperationalPerformanceSnapshot, User } from '../types';
import { mapStockRows, normalize, parseCsv } from '../services/operationalDataService';
import { hiddenRowsFromLegacyXls } from '../services/legacyXlsVisibility';
import { sellerPerformanceService } from '../services/sellerPerformanceService';
import { stockSnapshotService } from '../services/stockSnapshotService';
import { storeScopedOperationalService } from '../services/storeScopedOperationalService';

type Props={currentUser:User;companyId:string;storeId:string;storeName:string};
type ImportType='stock'|'performance';
const MONTHS=['JANEIRO','FEVEREIRO','MARÇO','ABRIL','MAIO','JUNHO','JULHO','AGOSTO','SETEMBRO','OUTUBRO','NOVEMBRO','DEZEMBRO'];
const localDate=()=>{const n=new Date();return `${n.getFullYear()}-${String(n.getMonth()+1).padStart(2,'0')}-${String(n.getDate()).padStart(2,'0')}`;};
const toNumber=(value:unknown)=>{const raw=String(value??'').trim().replace(/R\$/gi,'').replace(/%/g,'').replace(/\s/g,'');if(!raw||raw.startsWith('#'))return 0;if(raw.includes(',')&&raw.includes('.'))return Number(raw.replace(/\./g,'').replace(',','.'))||0;if(raw.includes(','))return Number(raw.replace(',','.'))||0;return Number(raw)||0;};
const toPercent=(value:unknown)=>{const n=toNumber(value);return Math.abs(n)<=1?n*100:n;};

const excelRows=async(file:File)=>{
 const buffer=await file.arrayBuffer();
 const hiddenLegacy=hiddenRowsFromLegacyXls(buffer);
 const wb=XLSX.read(buffer,{type:'array',cellStyles:true});
 const ws:any=wb.Sheets[wb.SheetNames[0]];
 const matrix=XLSX.utils.sheet_to_json<any[]>(ws,{header:1,defval:'',raw:true});
 const headerIndex=matrix.findIndex(r=>r.some((cell:any)=>['placa','vendedor','familia','modelo'].includes(normalize(String(cell)))));
 if(headerIndex<0)return [] as Record<string,string>[];
 const headers=matrix[headerIndex].map((h:any)=>normalize(String(h)));
 const rowsMeta:Array<{hidden?:boolean;hpt?:number;hpx?:number}>=Array.isArray(ws['!rows'])?ws['!rows']:[];
 return matrix.slice(headerIndex+1).map((row,offset)=>({row,sheetIndex:headerIndex+1+offset})).filter(({sheetIndex})=>{
   const meta=rowsMeta[sheetIndex];
   return !hiddenLegacy.has(sheetIndex)&&meta?.hidden!==true&&meta?.hpt!==0&&meta?.hpx!==0;
 }).map(({row})=>{const obj:Record<string,string>={};headers.forEach((h:string,i:number)=>{if(h)obj[h]=String(row[i]??'');});return obj;}).filter(row=>Object.values(row).some(Boolean));
};

const mapPerf=(row:any[]):OperationalPerformanceSeller|null=>{const seller=String(row[0]??'').trim();if(!seller)return null;return{seller,sellerKey:normalize(seller),passages:toNumber(row[1]),orders:toNumber(row[2]),flowTotal:toNumber(row[3]),orderPercent:toPercent(row[4]),workInPeriod:toNumber(row[5]),avgContactsPerDay:toNumber(row[6]),evaluations:toNumber(row[7]),evaluationRate:toPercent(row[8]),closing:toNumber(row[9]),syonetSales:toNumber(row[10]),closingPercent:toPercent(row[11]),marginPerCar:toNumber(row[12]),marginTotal:toNumber(row[13]),marginPercent:toPercent(row[14]),captureQty:toNumber(row[15]),capturePercent:toPercent(row[16]),pipeline:toNumber(row[17]),projection:toNumber(row[18]),additionalPurchase:toNumber(row[19])};};

const readPerformanceMap=async(file:File,referenceDate:string):Promise<OperationalPerformanceSnapshot>=>{
 const wb=XLSX.read(await file.arrayBuffer(),{type:'array'});const month=MONTHS[Math.max(0,Number(referenceDate.slice(5,7))-1)];const sheetName=wb.SheetNames.find(n=>normalize(n)===normalize(month))||wb.SheetNames[wb.SheetNames.length-1];const ws=wb.Sheets[sheetName];if(!ws)throw new Error(`Não encontrei a aba ${month} no arquivo.`);const matrix=XLSX.utils.sheet_to_json<any[]>(ws,{header:1,defval:'',raw:true});const headerIndex=matrix.findIndex(r=>r.some((c:any)=>normalize(String(c))==='vendedor'));if(headerIndex<0)throw new Error('Não encontrei o cabeçalho de vendedores no mapa.');const sellers:OperationalPerformanceSeller[]=[];let total:OperationalPerformanceSeller|undefined;let storeSectionIndex=-1;
 for(let i=headerIndex+1;i<matrix.length;i++){const row=matrix[i]||[];const first=normalize(String(row[0]??''));if(first==='indicadores da loja'||first==='indicadores do dia'){storeSectionIndex=i;break;}if(!first)continue;const mapped=mapPerf(row);if(!mapped)continue;if(mapped.sellerKey==='total')total=mapped;else sellers.push(mapped);}if(!sellers.length)throw new Error(`A aba ${sheetName} foi encontrada, mas nenhum vendedor foi reconhecido.`);
 const storeMetrics:Record<string,number|string>={};if(storeSectionIndex>=0){for(let r=storeSectionIndex+1;r<matrix.length;r++){const row=matrix[r]||[];for(let c=0;c<row.length;c++){if(typeof row[c]!=='string'||!row[c].trim())continue;const label=normalize(row[c]);if(!label||/^#/.test(label))continue;for(let n=c+1;n<=Math.min(c+2,row.length-1);n++){const value=row[n];if(value===''||value==null)continue;if(typeof value==='number'||!Number.isNaN(Number(String(value).replace(',','.')))){storeMetrics[label]=typeof value==='number'?value:Number(String(value).replace(',','.'));break;}}}}}
 return{referenceDate,sheetName,sellers,...(total?{total}:{}),storeMetrics};
};

const OperationalDataPanel:React.FC<Props>=({currentUser,companyId,storeId,storeName})=>{
 const[open,setOpen]=useState(false),[busy,setBusy]=useState(false);const[type,setType]=useState<ImportType>('stock');const[referenceDate,setReferenceDate]=useState(localDate);const[file,setFile]=useState<File|null>(null);const[rows,setRows]=useState<Record<string,string>[]>([]);const[performance,setPerformance]=useState<OperationalPerformanceSnapshot|null>(null);const[message,setMessage]=useState<{kind:'ok'|'error';text:string}|null>(null);
 const stockItems=useMemo(()=>mapStockRows(rows,referenceDate),[rows,referenceDate]);const preview=stockItems.slice(0,4);
 const reset=(next:ImportType)=>{setType(next);setFile(null);setRows([]);setPerformance(null);setMessage(null);};
 const handleFile=async(next:File|null)=>{setFile(next);setRows([]);setPerformance(null);setMessage(null);if(!next)return;try{const lower=next.name.toLowerCase();if(!/\.(csv|xls|xlsx)$/.test(lower))throw new Error('Envie CSV, XLS ou XLSX.');if(type==='performance'){if(lower.endsWith('.csv'))throw new Error('Para o Mapa de Indicadores, envie XLS ou XLSX.');setPerformance(await readPerformanceMap(next,referenceDate));}else{const parsed=lower.endsWith('.csv')?parseCsv(await next.text()):await excelRows(next);setRows(parsed);if(!parsed.length)throw new Error('Não encontrei uma tabela reconhecível no arquivo.');}}catch(error:any){setMessage({kind:'error',text:error?.message||'Não consegui ler esse arquivo.'});}};
 const importData=async()=>{if(!file)return;setBusy(true);setMessage(null);try{if(type==='stock'){const count=await stockSnapshotService.replace(stockItems,file.name,currentUser,storeId,companyId);setMessage({kind:'ok',text:`${storeName}: estoque atualizado com ${count} veículos em ${referenceDate}.`});}else{if(!performance)throw new Error('Mapa ainda não reconhecido.');const scoped={...performance,companyId,storeId};const count=await storeScopedOperationalService.importPerformance(scoped,file.name,currentUser,storeId,companyId);let linked=0;try{linked=await sellerPerformanceService.syncFromSnapshot(scoped,storeId,companyId);}catch{}setMessage({kind:'ok',text:`${storeName}: performance atualizada com ${count} vendedores · ${linked} vinculado(s).`});}setFile(null);setRows([]);setPerformance(null);window.dispatchEvent(new Event('dealmaster:operational-data-updated'));}catch(error:any){setMessage({kind:'error',text:error?.message||'Erro ao importar.'});}finally{setBusy(false);}};
 if(!['admin','manager'].includes(currentUser.role))return null;const recognized=type==='stock'?stockItems.length>0:!!performance;
 return <><button onClick={()=>setOpen(true)} className="fixed bottom-5 left-5 z-[130] flex items-center gap-2 rounded-full border border-white/10 bg-zinc-900 px-4 py-3 text-sm font-semibold text-white shadow-2xl"><Building2 size={18}/> Dados da Loja</button>{open&&<div className="fixed inset-0 z-[220] overflow-y-auto bg-black/75 p-3 backdrop-blur-md" onClick={()=>setOpen(false)}><div onClick={e=>e.stopPropagation()} className="mx-auto mt-5 max-w-4xl rounded-[32px] border border-white/10 bg-zinc-950 shadow-2xl"><div className="flex items-center justify-between border-b border-white/10 p-6"><div><p className="text-xs font-semibold uppercase tracking-[.15em] text-zinc-500">Atualização diária · Multi-Store</p><h3 className="mt-1 text-2xl font-semibold text-white">{storeName}</h3></div><button onClick={()=>setOpen(false)} className="grid h-10 w-10 place-items-center rounded-full bg-white/[.06] text-zinc-400"><X size={18}/></button></div><div className="p-6"><div className="grid gap-3 md:grid-cols-2"><button onClick={()=>reset('stock')} className={`rounded-[24px] border p-5 text-left ${type==='stock'?'border-white bg-white text-black':'border-white/10 bg-white/[.03] text-white'}`}><p className="text-xs font-semibold uppercase opacity-60">Arquivo 1</p><p className="mt-2 text-xl font-semibold">📦 Atualizar Estoque</p><p className="mt-2 text-sm opacity-60">Respeita as linhas ocultas do filtro DealerNet / LibreOffice.</p></button><button onClick={()=>reset('performance')} className={`rounded-[24px] border p-5 text-left ${type==='performance'?'border-white bg-white text-black':'border-white/10 bg-white/[.03] text-white'}`}><p className="text-xs font-semibold uppercase opacity-60">Arquivo 2</p><p className="mt-2 text-xl font-semibold">📊 Atualizar Performance</p></button></div><div className="mt-5 grid gap-4 md:grid-cols-[220px_1fr]"><input type="date" value={referenceDate} onChange={e=>setReferenceDate(e.target.value)} className="h-12 rounded-2xl border border-white/10 bg-white/[.05] px-4 text-white"/><label className="flex h-12 cursor-pointer items-center justify-between rounded-2xl border border-dashed border-white/15 bg-white/[.035] px-4 text-sm text-zinc-300"><span className="truncate">{file?.name||'Selecionar arquivo'}</span><FileUp size={17}/><input type="file" accept=".csv,.xls,.xlsx" className="hidden" onChange={e=>handleFile(e.target.files?.[0]||null)}/></label></div>{recognized&&<div className="mt-5 rounded-[24px] border border-white/10 bg-white/[.03] p-4"><div className="flex items-center justify-between"><div><p className="text-sm font-medium text-white">Arquivo reconhecido</p><p className="mt-1 text-xs text-zinc-500">{type==='stock'?`${stockItems.length} veículos visíveis/válidos`:`${performance?.sellers.length||0} vendedores · ${performance?.sheetName}`}</p></div><span className="rounded-full bg-emerald-500/10 px-3 py-1 text-xs text-emerald-400">Prévia OK</span></div>{type==='stock'&&<div className="mt-4 grid grid-cols-2 gap-2 text-xs md:grid-cols-4">{preview.map(item=><div key={item.id} className="rounded-xl bg-black/20 p-2 text-zinc-300">{item.plate}<br/><span className="text-zinc-500">{item.vehicle} · {item.stockDays} dias</span></div>)}</div>}</div>}{message&&<div className={`mt-5 flex gap-3 rounded-2xl border p-4 text-sm ${message.kind==='ok'?'border-emerald-500/20 bg-emerald-500/[.07] text-emerald-300':'border-red-500/20 bg-red-500/[.07] text-red-300'}`}>{message.kind==='ok'?<CheckCircle2 size={18}/>:<AlertTriangle size={18}/>}<span>{message.text}</span></div>}<div className="mt-6 flex justify-end"><button disabled={!file||!recognized||busy} onClick={importData} className="rounded-2xl bg-white px-6 py-3 text-sm font-semibold text-black disabled:opacity-30">{busy?'Atualizando...':type==='stock'?'Atualizar estoque':'Atualizar performance'}</button></div></div></div></div>}</>;
};
export default OperationalDataPanel;
